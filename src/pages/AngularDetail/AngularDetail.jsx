import './AngularDetail.scss';

const AngularDetail = (props) => {
    // const { id } = props.params.id;
    // console.log(id)

    return ( 
        <div className="angular-detail">
            <h1>Angular Detail</h1>
        </div>
     );
}
 
export default AngularDetail;